{
  "name": "driver-incentive-bonus-tracker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm.cmd run dev --workspace=frontend",
    "dev:backend": "npm.cmd run dev --workspace=backend",
    "dev": "concurrently \"npm.cmd run dev:backend\" \"npm.cmd run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
